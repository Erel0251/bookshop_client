import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#556cd6',
      light: '#b5c18e',
    },
    secondary: {
      main: '#dc004e',
      light: '#eadbc8',
    },
    background: {
      default: '#f1f1f1',
      paper: '#fff',
    },
  },
});
